package uk.ac.aber.dcs.rco1.converser.model.home

/**
 * Enum for identifying if a translation item has the same direction of translation as the item
 * first translated in a conversation (first) or second (second)
 *
 * @author Ruby Osborne (rco1)
 * @version 1.0 (release)
 */
enum class PositionInConversation {
    FIRST, SECOND
}